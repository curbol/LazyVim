-- No smart-splits plugin: using plain vim keymaps for window nav/move.
-- ctrl+arrows = navigate windows, ctrl+shift+arrows = move (swap) windows.
return {
  {
    "mrjones2014/smart-splits.nvim",
    enabled = false,
  },
}
